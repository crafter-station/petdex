{
  "name": "petdex-desktop-sidecar",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "bun build server.ts --target=node --outfile=server.js --minify",
    "start": "node server.js"
  }
}
